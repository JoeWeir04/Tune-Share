# GroupApp
Music Sharing web app, allows users to upload music videos, like and comment on posts on the site.
Built using Django.

How to run:

In terminal run:
pip install -r "requirements.txt"
python populate_MVapp.py
python manage.py runserver
